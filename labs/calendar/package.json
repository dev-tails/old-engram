{
  "name": "@engramhq/calendar",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/CalendarApp.ts --bundle --outfile=public/js/calendar.app.js",
    "dev": "yarn build --servedir=public"
  },
  "devDependencies": {
    "esbuild": "^0.14.23"
  }
}
