{
  "name": "@engramhq/auth",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/AuthApp.ts --bundle --outfile=public/js/auth-app.js",
    "dev": "yarn build --servedir=public"
  },
  "devDependencies": {
    "esbuild": "^0.14.23"
  }
}
